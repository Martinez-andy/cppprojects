#include <iostream>
#include <stdio.h>

int main() {
    // break and continue keywords

    for (int i = 0; i <= 20; i++) {
        if (i != 13) {
            printf("%i\n", i);  
        }
    }
    return 0;
}